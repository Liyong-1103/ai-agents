[project]
name = "ai-agents-lab"
version = "0.1.0"
description = "A disposable W2 exercise for observing reproducible project environments"
requires-python = ">=3.12,<3.13"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=8.4,<10",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

